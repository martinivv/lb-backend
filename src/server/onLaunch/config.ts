const mainConfig = async function () {
  /* ... */
}

/* ========================================== SEND FOR EXECUTION ========================================== */

export default function () {
  mainConfig().catch((e) => console.error(e))
}
